import 'dart:ui';

class PathOrginizationModel
{
  final String title;
  final String subTitle;
  final Color colors;
  const PathOrginizationModel(
      {
        required this.title ,
        required this.subTitle ,
        required this.colors}
      );
}